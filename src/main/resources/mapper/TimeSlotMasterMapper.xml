<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.unknownclinic.appointment.repository.TimeSlotMasterMapper">

    <!-- 全時間枠マスタ取得 -->
    <select id="findAll" resultType="com.unknownclinic.appointment.domain.TimeSlotMaster">
        SELECT *
        FROM time_slot_master
        ORDER BY id
    </select>

    <!-- IDで時間枠マスタ取得 -->
    <select id="findById" resultType="com.unknownclinic.appointment.domain.TimeSlotMaster">
        SELECT *
        FROM time_slot_master
        WHERE id = #{id}
        LIMIT 1
    </select>

</mapper>