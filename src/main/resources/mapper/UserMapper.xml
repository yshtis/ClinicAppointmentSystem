<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.unknownclinic.appointment.repository.UserMapper">

    <!-- 診察券番号でユーザー取得 -->
    <select id="findByCardNumber" resultType="com.unknownclinic.appointment.domain.User">
        SELECT *
        FROM users
        WHERE card_number = #{cardNumber}
        LIMIT 1
    </select>

    <!-- 診察券番号＋生年月日でユーザー取得 -->
    <select id="findByCardNumberAndBirthday" resultType="com.unknownclinic.appointment.domain.User">
        SELECT *
        FROM users
        WHERE card_number = #{cardNumber}
          AND birthday = #{birthday}
        LIMIT 1
    </select>

    <!-- 診察券番号＋生年月日でパスワード更新 -->
    <update id="updatePasswordByCardNumberAndBirthday">
        UPDATE users
        SET password = #{encodedPassword}
        WHERE card_number = #{cardNumber}
          AND birthday = #{birthday}
    </update>

    <!-- IDでユーザー取得 -->
    <select id="findById" resultType="com.unknownclinic.appointment.domain.User">
        SELECT *
        FROM users
        WHERE id = #{id}
        LIMIT 1
    </select>

    <!-- 新規登録 -->
    <insert id="insert" parameterType="com.unknownclinic.appointment.domain.User" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO users (card_number, birthday, name, password, phone_number)
    VALUES (#{cardNumber}, #{birthday}, #{name}, #{password}, #{phoneNumber})
	</insert>

    <!-- 管理用：全ユーザー一覧 -->
    <select id="findAll" resultType="com.unknownclinic.appointment.domain.User">
        SELECT *
        FROM users
        ORDER BY id
    </select>
    
    <!--
    診察券番号をID指定で更新するクエリ
    新規患者登録時、5桁連番＋ランダム2桁の診察券番号を後から付与する用途で使用
	-->
    <update id="updateCardNumber">
    	UPDATE users SET card_number = #{cardNumber} WHERE id = #{id}
	</update>

</mapper>